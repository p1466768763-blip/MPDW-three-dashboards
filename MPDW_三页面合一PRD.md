# MPDW 三页面合一 PRD（原型对齐版）

> 版本：v1.1｜日期：2026-09-03｜状态：原型评审与生产化需求基线
>
> 本文件以对话中指定的两个历史 Codex 任务为页面视觉与交互唯一来源：
> - 编导工作台：codex://threads/01a0379b-b56d-7923-af17-395345a0d3f4
> - 管理员看板 / 需求看板：codex://threads/01a03246-f572-7042-8693-2923bbfb17e6
>
> 三个离线 HTML 只封装原历史实现；没有重新设计布局、字段、颜色、文案或交互。下文附上工作区原始 PRD 全文，作为功能、字段、状态和逻辑的完整基线。

## 三页面原型对齐摘要

### 页面与职责

| 页面 | 历史入口/来源 | 角色 | 核心功能 |
| --- | --- | --- | --- |
| 管理员看板 | index.html / 需求进度 | 中台调度员、管理员 | 全局指标、按需求单聚合、展开明细、整单/单条分配、整单/单条撤销、审核台链接 |
| 需求看板 | user.html / 需求看板 | 制作人员 | 只看本人任务、需求单折叠/展开、明细查看、审核台链接 |
| 编导工作台 | 历史任务中的独立 HTML | 编导/审核协作人 | 6 张素材卡、每张 9 个成片版本、预览、上传、下载、删除、查看审核意见 |

### 管理员和需求看板的原型字段

`编导名称`、`需求视频总数`、`今日需交付数`、`今日已完成数`、`需求单号`、`模版名称`、`店铺名`、`需求人`、`难易度`、`视频数量`、`最后交付日期`、`二次修改数量`、`素材下载链接`、`审核链接`、`撤销`。

同一需求单号的多条明细默认折叠为汇总行。折叠时显示整单“审核台链接”，展开后每条明细仍显示“审核台链接”。管理员可打开待分配弹层，按需求单展开，执行整单或单条分配；撤销会把明细返回待分配池。需求看板复用相同表格，但只展示历史原型固定用户“祥子”的数据并隐藏管理操作。

### 编导工作台的原型字段与交互

- 顶栏：千机-短视频、编导工作台 · 成片审核、帮助、通知、祥子。
- 素材卡：素材编号、素材名称、需求单号、来源缩略图、9 个 CP-xx-Vx 成片版本。
- 版本状态：待审核、审核通过、需修改、审核不通过；无文件时显示待上传成片。
- 版本操作：上传、下载、删除（历史任务最终将“替换”改为“删除”）。
- 审核弹窗：视频预览、版本状态、只读审核意见；备注弹窗提示审核意见不能在此页修改。
- 离线封装将历史页面原有图片和视频转为 data URL，保持预览画面和视觉不变；上传文件只在当前浏览器会话中使用 object URL。

### 原型数据与同步逻辑

管理员/需求看板使用历史 `src/main.jsx` 的 sharedData：`groups` 保存已分配组和行，`unassignedDemands` 保存待分配明细，`sourceDemand` 用于撤销还原；同源页面使用 `localStorage`、`BroadcastChannel`、storage/focus/visibility 事件和 1 秒轮询同步。编导工作台使用内联 `materials` 数组，6 个素材各生成 9 个版本。

### 生产化状态与领域模型

正式生产建议沿用原始 PRD 的 DemandOrder、DemandItem、Assignment、MaterialAsset、DeliveryVersion、ReviewRecord 六类对象；明细状态为 `UNASSIGNED` 待分配、`ASSIGNED` 已分配、`IN_PROGRESS` 制作中、`PENDING_REVIEW` 待审核、`REWORK` 返修中、`APPROVED` 已完成、`CANCELLED` 已取消。分配、交付、审核以明细为最小执行单元，整单动作是对明细的批量事务；审核通过/驳回、返修次数、版本、权限和审计必须由服务端持久化校验。

### 重要原型边界

离线 HTML 不依赖本地服务，但不等于生产闭环：没有登录/服务端权限、真实 API/数据库、真实审核台 URL、跨设备同步、成片提交审核、审核通过/驳回写入、审计日志、并发锁、筛选分页和通知服务。原始 PRD 中带“规划需求”的内容需要业务和研发确认后实现。

---

## 1. 管理员看板

### 1.1 功能

1. 顶部品牌和业务导航，`中台需求管理` 作为下拉入口，子项为“需求进度”和“需求看板”。
2. 面包屑显示 `中台需求管理 / 需求进度`。
3. 需求动态概览卡：待分配需求数、今日需交付需求数、正在处理需求数。
4. 需求进度主表：按编导/运营线分组，再按需求单聚合；多明细需求单默认折叠。
5. 点击“分配需求”打开待分配需求弹层；弹层按需求单聚合，可展开明细。
6. 支持整单分配和单条分配；分配成功后从待分配池移除并追加到目标组。
7. 支持整单或单条“撤销”，将任务返回待分配池，并更新组汇总。
8. 点击模版名称显示打开需求的提示；点击“审核台链接”显示打开审核台链接的提示。
9. 底部固定横向滚动条保证宽表格可访问。

### 1.2 字段口径

| 序号 | 表头 | 当前原型显示含义 | 生产正式字段建议 |
| ---: | --- | --- | --- |
| 1 | 编导名称 | 分组第一列显示的编导花名 | `assignee.displayName`（建议正式命名为负责人） |
| 2 | 需求视频总数 | 分组需求视频总量 | `SUM(DemandItem.requestedVideoCount)` |
| 3 | 今日需交付数 | 分组今日节点数量 | `SUM(todayDueVideoCount)`，单位需确认 |
| 4 | 今日已完成数 | 分组今日完成数量 | `SUM(todayCompletedVideoCount)` |
| 5 | 需求单号 | 需求单编号；同单明细共享 | `DemandOrder.orderNo` |
| 6 | 模版名称 | 单条制作模版/类型 | `DemandItem.templateName` |
| 7 | 店铺名 | 需求所属店铺 | `DemandOrder.storeName` |
| 8 | 需求人 | 当前原型的需求人/制作组展示 | `DemandOrder.requesterId` / `requester.displayName` |
| 9 | 难易度 | 单条难度值 | `DemandItem.difficultyCode` |
| 10 | 视频数量 | 单条要求视频数量 | `DemandItem.requestedVideoCount` |
| 11 | 最后交付日期 | 当前原型日期/日期字符串 | `DemandItem.dueAt` 或 `DemandOrder.dueAt` |
| 12 | 二次修改数量 | 当前原型的修改数量列 | `DemandItem.revisionCount` |
| 13 | 素材下载链接 | 有链接时打开下载地址，否则显示“未配置链接” | 受权限控制的 `MaterialAsset` 下载动作 |
| 14 | 审核链接 | 历史表头名称；行内按钮文案为“审核台链接” | `ReviewWorkbench` deep link / review task URL |
| 15 | 撤销 | 管理员整单/单条返回待分配 | `Assignment` revoke/transfer action |

### 1.3 需求单聚合与待分配弹层

- 同一 `demandNo` 的多条明细合并为一个汇总行；汇总展示明细数、模版、店铺、需求人、难易度合计、视频数合计、最近交付日期、修改数量合计和素材链接数量。
- 点击数量按钮展开，明细行显示“明细 1、明细 2 …”并恢复单条字段；折叠状态只是呈现状态，不是业务状态。
- 折叠需求单时“审核台链接”按整单触发；展开后每条明细继续显示“审核台链接”。
- 待分配弹层表头：`需求单号`、`需求视频总数`、`今日已完成数`、`店铺名称`、`需求人`。整单分配会移动该单全部明细，单条分配只移动所选明细。
- 点击弹层外部或 Escape 关闭，不改变数据。

## 2. 需求看板

### 2.1 功能

1. 复用管理员看板的顶部导航、概览卡、宽表和需求单折叠/展开逻辑。
2. 只显示当前用户的任务范围；历史原型固定为 `祥子` 对应分组。
3. 隐藏管理员专属的分配和撤销操作。
4. 保留需求单汇总行和明细行的“审核台链接”。
5. 与管理员页共享浏览器演示数据：管理员分配后，个人页通过 `localStorage`、`BroadcastChannel`、storage/focus/visibility 事件和 1 秒轮询同步。

### 2.2 用户范围逻辑

- 原型通过页面入口判断个人页，并使用 `userShop = '祥子'` 过滤组和待分配数据。
- 生产版必须改为登录用户 `userId` 与授权组织范围过滤，不能用花名或 URL 作为权限边界。
- 用户页只展示自己的需求组；管理员页展示全部组和待分配池。

## 3. 编导工作台

### 3.1 页面组成

1. 顶部栏：`千机-短视频`、`编导工作台 · 成片审核`、帮助、通知、祥子头像。
2. 页面标题：`我的成片审核`，副文案说明当前审核队列。
3. 队列摘要：`祥子 · 当前审核队列`、待处理版本数、点击缩略图查看详情。
4. 素材审核清单：6 张素材卡片（素材 01–06），每张卡片 9 个成片版本。
5. 素材卡三栏：来源素材、成片预览、审核备注。
6. 成片操作：上传、下载、删除；“替换”已按历史任务最终反馈改为“删除”。
7. 审核弹窗：视频预览、版本状态、审核意见只读查看；点击缩略图或“查看详细审核意见”打开。
8. 帮助和通知以轻量 toast 反馈；不跳转外部服务。

### 3.2 字段与行为

| 区域 | 字段/文案 | 说明 |
| --- | --- | --- |
| 素材卡 | 素材编号 | 01–06，示例数据 |
| 素材卡 | 素材名称 | 秋季上新口播、达人素材二剪、女装通勤展示等 |
| 素材卡 | 需求单号 | UD-001、UD-002 等 |
| 成片版本 | 成片编号 | CP-01-V1 至 CP-06-V9 |
| 成片版本 | 状态 | 待审核、审核通过、需修改、审核不通过 |
| 成片版本 | 视频元信息 | 已生成待审核、已上传文件名、待上传成片 |
| 成片操作 | 上传 / 下载 / 删除 | 删除当前版本文件，可重新上传 |
| 审核备注 | 自动保存 / 已保存 | 当前页面只读展示审核意见 |
| 审核弹窗 | 成片审核 / 详细审核意见 | 显示编号、状态、视频和文字意见 |

- 每张素材固定渲染 9 个版本；离线封装只把历史图片/视频改为 data URL，画面和样式不改。
- 点击缩略图打开审核弹窗；无视频时提示先上传。上传校验视频类型后使用 `URL.createObjectURL` 仅在当前会话预览。
- 下载创建本地下载链接；删除释放 object URL，清空文件名并恢复“待上传成片”。
- 审核状态由示例 `approved` 集合和素材备注推导；当前原型没有审核人提交、通过/驳回表单。

## 4. 数据模型与页面数据关系

### 4.1 原型数据结构

```js
{
  groups: [{
    id, shop, line, totalTarget, totalCompleted, totalTodayCompleted,
    rows: [{ demandNo, type, difficulty, group, target, maintain,
      shoot, director, edit, todayCompleted, downloadUrl, sourceDemand }]
  }],
  unassignedDemands: [{ id, demandNo, name, type, total, difficulty,
    quantity, delivery, note, owner, shop }]
}
```

- `groups` 是管理员主表和需求看板的共同数据源。
- `unassignedDemands` 是待分配池；分配时移除并转换为已分配 `rows`。
- `sourceDemand` 保存待分配原始字段，撤销时用于还原需求。
- `sharedDataStorageKey = mpdw:demand-data:v3`；浏览器本地同步只用于原型演示。

### 4.2 生产领域模型（规划）

| 对象 | 关键字段 | 关系 |
| --- | --- | --- |
| `DemandOrder` 需求单 | `id`, `orderNo`, `sourceSystem`, `sourceOrderId`, `storeId`, `businessLineId`, `priority`, `dueAt`, `status` | 1 对多 `DemandItem` |
| `DemandItem` 需求明细 | `id`, `orderId`, `itemNo`, `templateId`, `requestedVideoCount`, `difficultyCode`, `dueAt`, `status`, `currentAssigneeId`, `revisionCount`, `version` | 最小执行单元 |
| `Assignment` 分配记录 | `itemId`, `assigneeId`, `teamId`, `action`, `operatorId`, `reason`, `assignedAt`, `endedAt`, `requestId` | 保留分配/转派/撤销历史 |
| `MaterialAsset` 素材 | `assetId`, `itemId`, `fileName`, `contentType`, `size`, `checksum`, `storageKey`, `version`, `availabilityStatus` | 明细可关联多个素材 |
| `DeliveryVersion` 交付版本 | `id`, `itemId`, `versionNo`, `submittedBy`, `submittedAt`, `remark`, `status` | 一条明细有多个版本 |
| `ReviewRecord` 审核记录 | `deliveryVersionId`, `reviewerId`, `result`, `reasonCode`, `comment`, `reviewedAt` | 一个版本一个有效最终结果 |

## 5. 状态定义与状态机

### 5.1 原型可见状态

| 页面 | 状态文案 | 触发/来源 |
| --- | --- | --- |
| 管理员/需求看板 | 待分配 | 明细位于 `unassignedDemands` 或负责人为空 |
| 管理员/需求看板 | 分配后列表存在 | 明细已加入目标 `group.rows` |
| 编导工作台 | 待审核 | 版本未进入 `approved`，且无需修改备注 |
| 编导工作台 | 审核通过 | 版本索引位于 `approved` 集合 |
| 编导工作台 | 需修改 | 素材有备注且当前版本为第一个未通过版本 |
| 编导工作台 | 审核不通过 | 状态样式预留，当前示例计数为 0 |
| 编导工作台 | 待上传成片 | 版本没有可用视频地址 |

### 5.2 生产状态（规划）

`UNASSIGNED` 待分配 → `ASSIGNED` 已分配 → `IN_PROGRESS` 制作中 → `PENDING_REVIEW` 待审核 → `APPROVED` 已完成；审核驳回进入 `REWORK` 返修中后再次提交；来源取消或管理员取消进入 `CANCELLED`。

规则：

1. 分配只能从 `UNASSIGNED` 进入 `ASSIGNED`，同一明细同一时刻只有一个主负责人。
2. 上传文件不等于提交审核；必须生成 `DeliveryVersion` 后才进入 `PENDING_REVIEW`。
3. 审核通过进入 `APPROVED`；驳回必须记录原因/意见并使 `revisionCount + 1`，进入 `REWORK`。
4. `PENDING_REVIEW` 默认不能直接撤销；`APPROVED` 默认不能直接回池，重开需要专门权限。
5. 需求单状态由明细聚合：全部待分配为待分配、全部完成为已完成、混合状态显示进行中并带数量。

## 6. 业务逻辑与验收规则

### 6.1 管理员分配

1. 管理员点击目标组的“分配需求”。
2. 系统打开待分配弹层并按需求单聚合。
3. 点击整单“分配需求”时，将该 `demandNo` 下全部可分配明细追加到目标组；点击明细时只追加一条。
4. 更新 `totalTarget`、`totalTodayCompleted`，从 `unassignedDemands` 移除，关闭弹层并显示 toast。
5. 生产版必须增加权限、版本校验、事务、幂等和审计；任一明细冲突时整单默认全部失败。

### 6.2 管理员撤销

1. 在汇总行点击撤销，作用于该需求单全部明细；明细行点击撤销只作用于该条。
2. 将目标行从 `group.rows` 移除，转换回待分配结构并加入 `unassignedDemands`。
3. 按来源需求的视频数量和今日完成数扣减组汇总。
4. 生产版需填写撤销原因、校验状态、结束旧 Assignment 并记录审计；不得物理删除历史记录。

### 6.3 审核台链接

- 表头保留“审核链接”；行内动作统一显示“审核台链接”。
- 折叠需求单：按钮目标为 `需求单号整单`。
- 展开需求单：每个明细按钮目标为该明细模版/需求。
- 当前原型点击后以 toast 反馈“已打开「…」审核台链接”；生产版接入真实 review task URL 或审核台路由。

### 6.4 编导审核

- 点击版本缩略图：打开视频预览弹窗，显示状态和审核意见。
- 点击“查看详细审核意见”：打开备注查看弹窗。
- 上传：浏览器本地选取视频、校验扩展名/MIME、替换该版本的临时对象 URL。
- 删除：释放对象 URL，清空视频文件名，按钮恢复可上传。
- 生产版需拆分“上传完成”“提交审核”“审核通过/驳回”三个动作，并持久化版本、文件和记录。

## 7. 指标口径

### 7.1 原型指标

- 管理员卡片“待分配需求数”按 `unassignedDemands.length` 计算，实际是待分配明细数量，不是 distinct 需求单数。
- 管理员“今日需交付需求数”当前使用示例常量 60；“正在处理需求数”当前使用示例常量 300。
- 需求看板个人卡片根据当前组行的 `target/edit` 和 `maintain/edit` 推导，属于演示口径。
- 编导队列摘要的待处理数按每个素材的 9 个版本和当前示例状态计算。

### 7.2 生产建议

| 指标 | 建议正式口径 |
| --- | --- |
| 待分配需求单数 | 含 `UNASSIGNED` 明细的 distinct `orderId` |
| 待分配明细数 | `status = UNASSIGNED` 的 `DemandItem` 数 |
| 待分配视频数 | 待分配明细 `requestedVideoCount` 求和 |
| 今日需交付视频数 | 今日时区内到期且未完成明细的视频数求和 |
| 制作中明细数 | `ASSIGNED`、`IN_PROGRESS`、`REWORK` 明细数 |
| 待审核明细数 | `PENDING_REVIEW` 明细数 |
| 已完成视频数 | 业务确认后按需求数量或审核通过文件数计算 |

## 8. 角色与权限

| 角色 | 可见范围 | 可执行动作 |
| --- | --- | --- |
| 中台调度员 | 授权组织/业务线全部需求 | 查看、整单/单条分配、撤销、转派 |
| 制作人员/编导 | 本人授权任务 | 查看、下载素材、打开审核台、上传/提交成片（生产版） |
| 审核人员 | 授权审核任务 | 预览、通过、驳回、填写审核意见（生产版） |
| 系统管理员 | 全局 | 配置组织、字典、权限、审计 |

原型中的 `user.html` / 需求看板不是安全边界；生产版必须由服务端按 `userId`、角色和组织范围鉴权，不能依赖隐藏按钮或文件名。

## 9. 原型边界与生产缺口

当前离线交付明确不包含：登录和服务端权限、真实 API/数据库、真实审核台跳转、上传持久化、审核结果写入、搜索/筛选/分页、通知服务、审计日志、并发锁和跨设备同步。

生产化优先级：

1. 冻结 DemandOrder/DemandItem/Assignment/MaterialAsset/DeliveryVersion/ReviewRecord 数据合同。
2. 接入身份、组织权限、需求接入幂等与真实待分配池。
3. 实现整单/单条分配、撤销、转派的事务和审计。
4. 实现素材受控下载、交付版本、提交审核、通过/驳回/返修。
5. 将指标从前端常量改为服务端统一计算，并补齐搜索、筛选、分页和通知。

## 10. 追溯来源

- 页面源代码：`C:/mpdw/src/main.jsx`、`C:/mpdw/src/styles.css`、`C:/mpdw/编导工作台.html`。
- 构建入口：`C:/mpdw/index.html`、`C:/mpdw/user.html`、`C:/mpdw/vite.config.js`。
- 原始生产化 PRD 基线见本文末尾附录；该基线中的“规划需求”不应被误读为离线原型已经实现。


---

## 附录 A：原始生产化 PRD 完整基线

# 千机短视频「中台需求分发与交付工作台」产品需求文档

| 文档项 | 内容 |
| --- | --- |
| 文档版本 | v1.0 评审稿 |
| 编制日期 | 2026-08-27 |
| 编制依据 | C:\mpdw 当前工作区源码、页面入口、样例数据与生产构建结果 |
| 产品阶段判断 | 可交互前端原型，尚不具备生产上线条件 |
| 建议产品名称 | 中台需求分发与交付工作台 |
| 当前页面名称 | 中台需求管理 / 需求进度 |
| 目标读者 | 产品、业务运营、中台调度、制作团队、审核团队、研发、测试 |

## 0. 阅读说明

本文是从当前前端工作区反向梳理出的产品 PRD。为避免把原型演示误写成正式业务能力，全文采用以下标记：

- 【现状事实】：可以从当前源码或构建结果直接确认。
- 【规划需求】：为了把原型升级为可上线产品而提出的需求。
- 【待确认】：当前工作区无法证明，需要业务、产品或技术负责人决策。

本文不把样例中的人员、店铺、数量、难易度和交付日期视为真实生产数据，也不把样例阈值视为业务规则。

---

## 1. 产品摘要

### 1.1 产品定位

面向短视频中台调度、视频制作人员和审核人员，提供从“需求接入、待分配、分派执行、素材获取、成片提交、审核返修到完成归档”的统一工作台。

产品以“需求单”为业务主单元。一个需求单可包含多条需求明细，每条明细对应一种模版或制作要求。当前原型已经支持整单分配和单条分配；生产模型建议保留两种方式，是否允许同单明细分给不同负责人仍需业务确认。

### 1.2 当前工作区结论

【现状事实】当前项目已经演示以下核心交互：

1. 管理端查看全局需求进度。
2. 普通用户入口查看个人需求进度。
3. 待分配需求按需求单聚合，并可展开查看明细。
4. 管理端可以整单分配或分配单条需求。
5. 已分配需求按需求单聚合，可展开查看模版明细。
6. 管理端可以整单撤销或撤销单条需求，使其返回待分配池。
7. 管理端与用户端可通过浏览器本地存储和跨标签页机制同步演示数据。
8. 页面提供素材下载入口、成片上传与交付审核入口，但后两者目前只显示提示信息。

【现状事实】当前项目仍是纯前端样机：

- 没有后端服务、正式数据库和 API。
- 没有登录、组织、角色和服务端权限校验。
- 没有真实的成片上传、审核、驳回、返修和完成闭环。
- 没有真实业务数据接入、查询、筛选、分页、通知和审计日志。
- 部分展示字段复用了旧数据字段，字段名与数据语义不一致。
- 没有自动化测试、静态检查或端到端测试脚本。

因此，当前版本适合用于交互评审和业务对齐，不应直接接入生产数据上线。

### 1.3 一句话目标

让每一条短视频需求都能回答五个问题：从哪里来、由谁负责、何时交付、当前处于什么状态、最终交付和审核结果是什么。

### 1.4 MVP 成功标准

【规划需求】MVP 上线后应满足：

- 所有正式需求均拥有稳定且可追溯的需求单号和明细 ID。
- 所有分配、撤销、转派、提交和审核动作均由服务端鉴权并留下审计记录。
- 制作人员只能看到授权范围内的需求，调度人员只能操作授权组织范围内的数据。
- 看板指标由同一套后端状态和时间口径实时计算，不再使用前端常量。
- 成片提交后必须经过明确的审核结果，不能仅通过点击按钮视为完成。
- 跨页面、跨账号的数据变化最终一致，冲突操作可被识别并提示。

业务改善指标见第 12 章。目标数值应在获得真实基线后确认，本文不虚构固定目标。

---

## 2. 项目扫描与现状审计

### 2.1 工程概况

| 项目 | 扫描结果 |
| --- | --- |
| 技术栈 | React、React DOM、Vite、Lucide React |
| 项目版本 | package.json 标记为 0.1.0 |
| 页面入口 | index.html 管理端；user.html 普通用户端 |
| 构建方式 | Vite 多页面构建 |
| 核心业务文件 | src/main.jsx，约 1194 行 |
| 样式文件 | src/styles.css，约 257 行 |
| 数据持久化 | 浏览器 localStorage |
| 页面间同步 | BroadcastChannel、storage 事件、窗口聚焦/可见性刷新、1 秒轮询兜底 |
| 后端/API | 未发现 |
| 身份认证/权限 | 未发现 |
| 自动化测试 | 未发现 |
| Git 元数据 | 当前目录不是 Git 仓库 |
| 构建验证 | 2026-08-27 执行 npm.cmd run build，构建成功 |

### 2.2 样例数据规模

以下数字仅说明原型覆盖的展示规模，不代表真实业务量：

- 已分配展示组：12 组。
- 已分配样例明细：50 行。
- 待分配样例需求单：12 单。
- 待分配样例明细：117 条。
- 每个待分配样例单包含 5 至 15 条明细，这是种子数据生成方式，不是经确认的业务限制。
- 样例数据将展示组名称循环映射为 4 个员工花名，将店铺循环映射为 4 个店铺名。
- 当前“待分配需求数”卡片按明细数组长度统计，因此样例会显示 117，而不是 12 个需求单；这正是生产版必须拆分“单数、明细数、视频数”口径的原因。

### 2.3 当前能力与生产缺口

| 模块 | 当前原型能力 | 生产缺口 | PRD 处理 |
| --- | --- | --- | --- |
| 页面入口 | 管理端和普通用户端两个 HTML 入口 | 入口不是身份和权限边界 | P0 接入身份、组织和服务端授权 |
| 顶部导航 | 展示多个业务模块 | 除当前模块外按钮没有路由行为 | MVP 只保证本模块，其他导航保持壳层或接正式路由 |
| 需求概览 | 展示待分配、今日交付、处理中指标 | 管理端两个指标为常量；个人指标依赖语义混乱的旧字段 | P0 建立统一指标口径和后端聚合 |
| 待分配池 | 按需求单聚合，可展开明细 | 无筛选、搜索、分页、数据来源和并发控制 | P0 建设正式需求池 |
| 需求分配 | 支持整单和单条分配 | 只改浏览器内存/本地存储；无权限、锁、失败回滚和操作理由 | P0 建设原子分配接口和审计 |
| 已分配列表 | 按人员/运营线展示，按需求单折叠 | 字段映射存在歧义；无状态、筛选、排序、分页 | P0 重建数据合同和进度列表 |
| 个人看板 | 固定查看“祥子”的第一组数据 | 不是登录用户隔离；同名多组数据会遗漏 | P0 按登录用户 ID 查询全部授权数据 |
| 素材下载 | 有 URL 时可打开新窗口，无 URL 显示未配置 | 无文件权限、有效期、版本、下载审计 | P0 建设素材资产模型和受控下载 |
| 成片交付 | 展示“上传成片并交付审核”按钮 | 点击只出现提示，无文件上传和审核记录 | P0 建设交付版本与审核闭环 |
| 撤销回池 | 管理端可按单或明细撤销 | 无确认、理由、状态限制和并发校验；旧样例行可能无法正确扣减汇总 | P0 建设有约束的撤销/转派 |
| 实时同步 | 同源标签页之间同步本地数据 | 仅限单浏览器、单设备；不能多账号共享 | P0 由服务端事件或短轮询同步 |
| 适配 | 桌面宽表格和固定横向滚动条 | 页面最小宽度 1200px | MVP 仅支持桌面 Web |
| 质量保障 | 生产构建成功 | 无单元、集成、端到端和可访问性测试 | P0 建立核心流程测试与发布门禁 |

### 2.4 必须先治理的数据语义问题

【现状事实】当前表头已经改成需求业务字段，但底层仍沿用旧变量名和旧样例数据。现有映射包括：

| 当前展示字段 | 当前代码承载字段 | 已发现问题 |
| --- | --- | --- |
| 名称 | group.shop | 实际被改写为员工花名，不是店铺 |
| 运营线 | group.line | 仅作为名称下方副信息展示 |
| 需求视频总数 | group.totalTarget | 新分配时累加 demand.total，但旧样例含义不确定 |
| 今日需交付数 | group.totalCompleted | 字段名和展示口径不一致，且不会随分配动作更新 |
| 店铺名 | row.difficulty | 旧字段被复用为店铺字符串 |
| 负责人 | row.group | 旧样例值与待分配新行值并不统一 |
| 难易度 | row.target | 旧样例中可出现 310 等明显非难易度值 |
| 视频数量 | row.maintain | 旧样例可能表示流程数量，新分配行才表示需求数量 |
| 最后交付日期 | row.shoot | 旧样例中既可能是数字，也可能是 MM/DD 字符串 |
| 二次修改数量 | row.director | 旧样例含义不确定，不等同于真实返修轮次 |
| 完成量 | row.edit | 不直接显示，却参与个人指标计算 |

【规划需求】生产化开发前必须冻结新的领域模型和 API 字段，不得继续依赖这些兼容映射。

---

## 3. 背景与用户问题

### 3.1 业务背景假设

【待确认】根据页面名称、导航结构和交互推断，短视频中台会持续接收来自店铺或业务线的视频制作需求。每个需求单可能包含多个模版或视频任务，需要由调度人员分发给不同制作人员，并跟踪素材、交付日期、成片版本和修改情况。

### 3.2 需要解决的问题

1. 需求入口分散，需求单与明细缺少统一主键和统一状态。
2. 中台无法快速判断还有多少需求待分配、今日需要交付多少、谁的任务正在处理中。
3. 一个需求单包含多条明细时，整单调度和拆单调度容易发生遗漏或重复分配。
4. 制作人员缺少只看自己的工作台，容易依赖群聊或表格确认任务。
5. 素材下载、成片提交、审核和返修信息未形成同一条可追溯链路。
6. 撤销和转派缺少权限、原因和审计，责任边界不清晰。
7. 当前指标和字段口径不稳定，无法支持管理决策。

---

## 4. 产品目标与范围

### 4.1 产品目标

#### G1：统一需求台账

每个需求单及其明细具有稳定身份、明确来源、明确店铺、明确制作要求、明确截止时间和可追溯变更历史。

#### G2：提升分配效率和准确性

调度人员可以按整单或按明细分配，系统自动阻止重复分配和并发覆盖，并能看见负责人和任务状态。

#### G3：建立交付审核闭环

制作人员基于任务获取素材、提交成片；审核人员给出通过或驳回结果；所有版本和反馈均可追溯。

#### G4：提供可信进度指标

全局与个人看板使用同一套状态、截止时间和组织权限口径。

#### G5：支持生产运营

提供搜索、筛选、排序、分页、异常提示、通知、操作日志和稳定的跨账号数据同步。

### 4.2 MVP 范围

MVP 包含：

- 正式身份与角色权限。
- 需求接入与校验。
- 待分配需求池。
- 整单和单条分配。
- 全局需求进度和个人需求进度。
- 素材资产访问。
- 成片上传、提交审核、审核通过、驳回返修、再次提交。
- 撤销和转派。
- 统一指标口径。
- 关键通知与审计。
- 基础筛选、搜索、排序和分页。

### 4.3 MVP 非目标

以下内容不进入首个生产 MVP：

- 自动生成短视频、自动剪辑或 AI 创意生产。
- 内容发布、爆款库、财务、团队绩效等其他导航模块的重做。
- 复杂排班、智能产能预测和自动派单。
- 对外客户门户。
- 以浏览器 localStorage 作为正式数据源。

---

## 5. 用户角色与权限

### 5.1 角色定义

| 角色 | 核心诉求 | 默认数据范围 |
| --- | --- | --- |
| 中台调度员 | 查看待分配池、分配、撤销、转派、跟踪整体进度 | 被授权组织/业务线内全部需求 |
| 制作人员 | 查看自己的任务、下载素材、更新开工状态、提交成片、处理返修 | 分配给本人或本人所在协作组的需求 |
| 审核人员 | 查看待审核成片、预览、通过或驳回、填写反馈 | 被授权业务线或店铺的审核任务 |
| 系统管理员 | 配置组织、角色、业务字典、集成和全局审计 | 系统级 |
| 只读管理者（P1） | 查看进度与报表，不执行业务动作 | 被授权组织范围 |

【待确认】审核人员是否为独立角色，还是由中台调度员兼任。

### 5.2 权限矩阵

| 操作 | 调度员 | 制作人员 | 审核人员 | 管理员 | 只读管理者 |
| --- | --- | --- | --- | --- | --- |
| 查看全局需求 | 授权范围内 | 否 | 审核范围内 | 是 | 授权范围内 |
| 查看待分配池 | 是 | 否 | 否 | 是 | 可配置只读 |
| 分配/整单分配 | 是 | 否 | 否 | 是 | 否 |
| 撤销/转派 | 是 | 否 | 否 | 是 | 否 |
| 下载素材 | 是 | 自己任务 | 审核任务 | 是 | 可配置 |
| 提交成片 | 可配置 | 自己任务 | 否 | 可配置 | 否 |
| 审核通过/驳回 | 否或可配置 | 否 | 是 | 是 | 否 |
| 查看操作日志 | 授权范围内 | 自己相关 | 自己相关 | 是 | 授权范围内 |
| 配置字典与权限 | 否 | 否 | 否 | 是 | 否 |

权限必须在服务端执行。隐藏前端按钮不能替代鉴权。

---

## 6. 核心业务对象

### 6.1 对象层级

~~~mermaid
erDiagram
    DEMAND_ORDER ||--|{ DEMAND_ITEM : contains
    DEMAND_ITEM ||--o{ ASSIGNMENT : assignment_history
    DEMAND_ITEM ||--o{ MATERIAL_ASSET : uses
    DEMAND_ITEM ||--o{ DELIVERY_VERSION : submits
    DELIVERY_VERSION ||--o{ DELIVERY_FILE : contains
    DELIVERY_VERSION ||--o| REVIEW_RECORD : reviewed_by
    USER ||--o{ ASSIGNMENT : receives
    TEAM ||--o{ USER : contains
    STORE ||--o{ DEMAND_ORDER : owns
~~~

### 6.2 数据对象定义

#### 6.2.1 DemandOrder：需求单

| 字段 | 类型建议 | 必填 | 定义 |
| --- | --- | --- | --- |
| id | UUID/雪花 ID | 是 | 系统内部不可变主键 |
| orderNo | String | 是 | 面向用户展示的需求单号，全局唯一 |
| sourceSystem | String | 是 | 需求来源系统 |
| sourceOrderId | String | 是 | 来源系统主键，用于幂等 |
| storeId/storeName | ID/String | 是 | 需求所属店铺 |
| businessLineId | ID | 是 | 所属业务线 |
| priority | Enum | 是 | 普通、加急等，字典可配置 |
| note | Text | 否 | 需求单备注 |
| dueAt | DateTime | 是 | 需求单最晚交付时间；明细可覆盖 |
| status | Enum | 是 | 根据明细状态聚合或独立管理 |
| createdAt/createdBy | DateTime/ID | 是 | 创建审计 |
| updatedAt/version | DateTime/Integer | 是 | 并发控制 |

建议唯一约束：sourceSystem + sourceOrderId。若来源允许同一主单多版本，则增加 sourceVersion。

#### 6.2.2 DemandItem：需求明细

| 字段 | 类型建议 | 必填 | 定义 |
| --- | --- | --- | --- |
| id | UUID/雪花 ID | 是 | 明细不可变主键 |
| orderId | ID | 是 | 所属需求单 |
| itemNo | String | 是 | 单内明细编号 |
| templateId/templateName | ID/String | 是 | 模版身份与展示名 |
| requestedVideoCount | Integer | 是 | 本明细要求交付的视频数量 |
| difficultyCode | Enum/String | 是 | 难易度业务字典值 |
| dueAt | DateTime | 否 | 明细最晚交付时间；为空则继承需求单 |
| requirementText | Text | 否 | 制作要求 |
| status | Enum | 是 | 明细生命周期状态 |
| currentAssigneeId | ID | 否 | 当前负责人，未分配时为空 |
| currentTeamId | ID | 否 | 当前制作组/运营线 |
| revisionCount | Integer | 是 | 已发生的返修轮次，初始为 0 |
| version | Integer | 是 | 乐观锁版本 |

#### 6.2.3 Assignment：分配记录

记录每次分配、转派和撤销，不覆盖历史。至少包含 itemId、assigneeId、teamId、action、operatorId、reason、assignedAt、endedAt 和 requestId。

#### 6.2.4 MaterialAsset：需求素材

至少包含 assetId、itemId/orderId、fileName、contentType、size、checksum、storageKey、version、uploadedBy、createdAt、availabilityStatus。前端使用短时有效的签名下载地址，不直接存储永久公开 URL。

#### 6.2.5 DeliveryVersion：交付版本

每次提交产生一个只增不改的版本，包含 versionNo、itemId、submittedBy、submittedAt、remark、status 和文件列表。返修后重新提交必须生成新版本。

#### 6.2.6 ReviewRecord：审核记录

包含 deliveryVersionId、reviewerId、result、reasonCode、comment、reviewedAt。驳回时 reasonCode 和 comment 至少有一项必填，具体规则待业务确认。

### 6.3 页面字段的正式口径

| 页面字段 | 正式字段/算法 | 口径要求 |
| --- | --- | --- |
| 名称 | assignee.displayName | 建议改名为“负责人”；运营线单独展示 |
| 需求视频总数 | SUM(requestedVideoCount) | 明确是视频数量，不是需求明细条数 |
| 今日需交付数 | 截止时间为今日且未完成的视频数量 | 若业务要看需求条数，应另列，不混用 |
| 需求单号 | DemandOrder.orderNo | 同单多明细共用 |
| 模版名称 | DemandItem.templateName | 模版需有稳定 ID |
| 店铺名 | DemandOrder.storeName | 不允许承载在 difficulty 字段 |
| 负责人 | User.displayName | 使用用户 ID 做关联，名称仅展示 |
| 难易度 | DemandItem.difficultyCode | 字典、等级范围和展示文案待确认 |
| 视频数量 | DemandItem.requestedVideoCount | 正整数 |
| 最晚交付时间 | DemandItem.dueAt 或 Order.dueAt | 建议将“最后交付日期”改为更明确的“最晚交付时间” |
| 返修次数 | DemandItem.revisionCount | 建议替代含义模糊的“二次修改数量” |
| 素材 | MaterialAsset 列表 | 显示数量、版本、可用状态 |
| 交付 | DeliveryVersion 操作 | 是动作，不是数据字段 |
| 撤销/转派 | Assignment 操作 | 是受权限和状态约束的动作 |

【待确认】“今日需交付数”的业务单位究竟是需求单、明细条数还是视频条数。本文建议默认使用视频条数，并在卡片副标题同时展示需求明细数。

---

## 7. 生命周期与状态机

### 7.1 主流程

~~~mermaid
flowchart LR
    A[上游系统或人工导入需求] --> B{字段与幂等校验}
    B -->|失败| C[异常队列<br/>补充或修正]
    B -->|成功| D[待分配池]
    D --> E{调度方式}
    E -->|整单| F[整单分配]
    E -->|单条| G[明细分配]
    F --> H[制作人员任务列表]
    G --> H
    H --> I[下载素材并开始制作]
    I --> J[上传成片并提交审核]
    J --> K{审核}
    K -->|通过| L[完成归档]
    K -->|驳回| M[返修中]
    M --> J
    H -->|符合撤销规则| D
~~~

### 7.2 明细状态

| 状态码 | 页面文案 | 进入条件 | 可执行动作 |
| --- | --- | --- | --- |
| UNASSIGNED | 待分配 | 需求校验通过但没有有效负责人 | 分配、取消 |
| ASSIGNED | 已分配 | 分配成功 | 开始制作、转派、撤销 |
| IN_PROGRESS | 制作中 | 制作人员确认开工，或发生可证明的制作动作 | 提交审核、转派、撤销 |
| PENDING_REVIEW | 待审核 | 成片版本提交成功 | 审核通过、驳回 |
| REWORK | 返修中 | 审核驳回 | 重新提交、转派、撤销 |
| APPROVED | 已完成 | 最新交付版本审核通过 | 查看、归档；重开需特殊权限 |
| CANCELLED | 已取消 | 来源取消或有权限人员取消 | 查看审计 |

### 7.3 状态转换规则

1. 所有状态转换由服务端校验当前状态和记录版本。
2. 分配动作仅允许从 UNASSIGNED 进入 ASSIGNED。
3. 同一明细同一时刻只能有一个有效主负责人；协作人另建关系，不复用负责人字段。
4. 提交审核必须至少存在一个上传成功且校验通过的成片文件。
5. 审核只针对当前有效交付版本；旧版本不可重复审核。
6. 审核驳回后 revisionCount 增加 1，进入 REWORK。
7. APPROVED 默认不可直接撤销回池；如业务需要重开，必须使用“重开”动作并记录原因。
8. PENDING_REVIEW 默认不能直接撤销；需先取消审核或由有特殊权限的人员执行强制撤销。
9. 整单状态由明细聚合展示：全部完成为已完成；全部待分配为待分配；混合状态显示进行中并展示各状态数量。

---

## 8. 信息架构与页面设计

### 8.1 管理端：需求进度

页面路径建议：/demand-management/progress

页面区域：

1. 面包屑与页面标题。
2. 全局指标卡。
3. 筛选工具栏。
4. 需求进度主表。
5. 待分配需求抽屉/弹层。
6. 需求详情抽屉。
7. 操作结果与异常提示。

筛选项至少包括：

- 需求单号/模版名称关键词。
- 状态。
- 负责人。
- 制作组/运营线。
- 店铺。
- 优先级。
- 难易度。
- 最晚交付时间范围。
- 是否逾期。

主表建议按“负责人/运营线 → 需求单 → 明细”三级展示。默认显示需求单汇总，展开后显示明细。列支持显隐配置，关键列固定，列表使用服务端分页或虚拟滚动。

### 8.2 管理端：待分配池

当前原型使用悬浮弹层。生产版可保留轻量分配弹层，但在数据量较大时建议使用右侧抽屉或独立页面，避免 1000 条以上数据在窄弹层中难以检索。

待分配池必须支持：

- 按需求单聚合。
- 展开明细。
- 整单分配。
- 勾选多条明细分配。
- 搜索和筛选。
- 展示截止时间、优先级、店铺、模版、视频数、难易度和素材就绪状态。
- 展示目标负责人的当前任务量或容量提示（P1）。

### 8.3 个人端：我的需求

页面路径建议：/my-demands

页面显示登录用户所有有效任务，而不是固定名称或第一条匹配数据。

页面区域：

1. 今日需交付、制作中、待审核、返修中、逾期等个人指标。
2. 我的任务筛选。
3. 按需求单聚合的任务列表。
4. 素材和制作要求详情。
5. 开始制作、上传成片、提交审核、查看审核反馈等动作。

### 8.4 审核端：待审核

页面路径建议：/delivery-reviews

审核列表必须展示：

- 需求单号、店铺、模版、负责人。
- 交付版本号、提交时间、截止时间。
- 成片预览和下载。
- 历史版本与历史反馈。
- 通过、驳回动作。
- 驳回原因和审核备注。

### 8.5 系统配置

MVP 可以只提供最低限度的管理配置：

- 人员、团队、业务线与角色映射。
- 店铺字典。
- 难易度字典。
- 优先级字典。
- 审核驳回原因字典。
- 文件类型、大小和保留期配置。
- 上游数据源配置。

---

## 9. 功能需求

### FR-P0-01 身份认证与数据权限

【规划需求】

- 接入企业统一身份或现有账号体系，具体方式待技术方案确认。
- 前端不再通过 URL 或固定花名确定身份。
- 后端根据 userId、role、organizationScope 和 businessLineScope 返回数据。
- 管理端、个人端、审核端使用相同身份上下文。
- 每个写操作校验动作权限、数据范围和目标对象状态。

验收标准：

1. 普通制作人员直接请求他人任务接口时返回 403。
2. 普通制作人员看不到待分配池和管理操作。
3. 同一用户属于多个制作组时，个人页可看到全部有效授权任务。
4. 调度员只能查看和操作授权业务线内需求。
5. 操作日志记录真实 userId，不使用页面传入的展示名作为身份。

### FR-P0-02 需求接入与幂等校验

【规划需求】

- 支持从上游系统通过 API、消息或批量导入接入需求，具体主方式待确认。
- 一个需求单至少包含一条需求明细。
- 必填字段缺失、店铺无映射、模版无映射、视频数非法、截止时间非法时进入异常队列，不进入待分配池。
- 重复接收同一来源需求时保证幂等。
- 上游更新需求时保留变更前后差异和版本。
- 上游取消需求时按当前状态执行取消规则；已提交或已完成需求不可静默删除。

验收标准：

1. 同一幂等键重复提交两次，只生成一个需求单。
2. 含 10 条明细的需求单接入后，主单和 10 条明细关系完整。
3. 任一明细校验失败时，系统按配置执行整单拒绝或部分接入；该策略必须在开发前冻结。
4. 异常记录包含可理解的错误原因和原始来源 ID。

### FR-P0-03 待分配需求池

【规划需求】

- 默认按需求单聚合展示，显示需求单数和明细数，不能混称“需求数”。
- 支持展开明细、搜索、筛选、排序和分页。
- 支持查看制作要求、素材就绪状态和截止时间。
- 对加急、今日到期、已逾期、素材缺失等情况提供明确标识。
- 池内数据发生变化时刷新结果，并保留用户可恢复的筛选条件。

验收标准：

1. 聚合行数量等于符合条件的 distinct orderId 数。
2. 展开后的明细数量与后端返回的 itemCount 一致。
3. 分配成功后，相应明细从待分配池消失；同单剩余明细仍保留。
4. 无数据、加载失败、权限不足和数据冲突有不同的状态提示。

### FR-P0-04 分配

【规划需求】

- 支持整单分配、单条分配和勾选多条明细分配。
- 目标可为负责人，并可同时指定制作组/运营线。
- 整单分配默认采用事务：全部成功或全部失败。
- 服务端校验明细仍为 UNASSIGNED 且版本未变化。
- 操作成功后写入 Assignment 历史并推送给相关用户。
- 分配弹层展示目标人员基础任务量；智能容量建议进入 P1。

验收标准：

1. 两名调度员同时分配同一明细时，只允许一个请求成功；另一请求返回冲突并提示刷新。
2. 整单含任一不可分配明细时，整单操作不产生部分成功，除非用户明确切换为“仅分配可用项”。
3. 单条分配不影响同需求单其他未分配明细。
4. 分配成功后，个人页能在约定同步时限内看到任务。
5. 每次分配均记录操作者、目标负责人、时间、来源状态和请求 ID。

### FR-P0-05 需求进度列表

【规划需求】

- 管理端查看授权范围内全部需求，个人端查看本人全部任务。
- 支持按需求单折叠和展开。
- 汇总字段从明细实时计算，不直接信任前端累加值。
- 展示需求状态、截止时间、是否逾期、素材状态、最新交付版本和审核状态。
- 支持服务端分页、稳定排序和 URL 可复现的筛选参数。

验收标准：

1. 同一需求单的多条明细能正确聚合，且汇总与展开明细求和一致。
2. 用户拥有多个组或多条业务线任务时不丢数据。
3. 分配、撤销、提交、审核后汇总数据同步变化。
4. 刷新浏览器或换设备登录后数据不丢失。

### FR-P0-06 素材管理与下载

【规划需求】

- 需求单或明细可关联多个素材文件和版本。
- 页面显示素材数量、名称、大小、类型、上传时间和可用状态。
- 下载前执行权限校验，返回短时签名地址。
- 记录关键下载行为。
- 素材缺失、处理中、已失效、无权限和下载失败分别提示。

验收标准：

1. 无权限用户无法通过复制链接下载素材。
2. 签名地址过期后不可继续访问，可在页面重新申请。
3. 新版本素材上传后，历史版本仍按保留策略可追溯。
4. 下载日志可关联用户、需求、素材和时间。

### FR-P0-07 成片上传与提交审核

【规划需求】

- 制作人员可为自己的任务上传一个或多个成片文件。
- 支持上传进度、失败重试、取消和断点续传能力评估。
- 文件类型、单文件大小、总大小、时长、分辨率等规则配置化；具体阈值待确认。
- 上传完成不等于提交审核。用户需确认文件列表和备注后提交。
- 每次提交生成新的 DeliveryVersion。
- 提交成功后明细进入 PENDING_REVIEW。

验收标准：

1. 没有有效成片文件时不能提交审核。
2. 上传失败不会生成可审核版本。
3. 重复点击提交使用幂等键，不生成重复版本。
4. 返修提交生成递增的新版本，旧版本和旧反馈仍可查看。
5. 非当前负责人不能提交，特殊代交权限除外。

### FR-P0-08 审核与返修

【规划需求】

- 审核人员可预览当前版本和历史版本。
- 审核结果为通过或驳回。
- 驳回必须记录结构化原因和/或文字反馈，规则待确认。
- 通过后明细进入 APPROVED。
- 驳回后进入 REWORK，revisionCount 增加，通知当前负责人。
- 同一版本只能产生一个有效最终审核结果。

验收标准：

1. 两名审核人同时审核同一版本时，仅第一笔有效提交成功。
2. 驳回后个人页能看到反馈和待返修状态。
3. 通过后任务从制作中指标移出，进入已完成口径。
4. 审核记录不可由普通用户修改或删除。

### FR-P0-09 撤销、转派与重开

【规划需求】

- 调度员可以在允许状态下将单条或整单撤销回待分配池。
- 撤销和转派必须填写原因。
- 已产生上传版本时保留文件和历史，不做物理删除。
- PENDING_REVIEW 默认先取消审核再撤销。
- APPROVED 默认不允许撤销；如确需变更，使用单独的“重开”权限和流程。
- 转派通过结束旧 Assignment 并创建新 Assignment 实现。

验收标准：

1. 撤销后当前负责人清空，状态回到 UNASSIGNED，历史分配记录仍存在。
2. 整单撤销只作用于满足规则的明细；若要求事务模式，任一不可撤销则整单失败。
3. 转派后旧负责人失去写权限，新负责人获得任务。
4. 任何撤销、转派、重开均可在审计日志中查看原因。

### FR-P0-10 指标与口径

【规划需求】

全局与个人指标必须使用同一算法，仅数据范围不同。

| 指标 | 建议定义 |
| --- | --- |
| 待分配需求单数 | 状态含 UNASSIGNED 明细的 distinct orderId 数 |
| 待分配明细数 | status = UNASSIGNED 的 DemandItem 数 |
| 待分配视频数 | UNASSIGNED 明细 requestedVideoCount 求和 |
| 今日需交付视频数 | 有效截止时间落在业务时区今日、状态非 APPROVED/CANCELLED 的 requestedVideoCount 求和 |
| 制作中明细数 | status in ASSIGNED, IN_PROGRESS, REWORK 的明细数 |
| 待审核明细数 | status = PENDING_REVIEW 的明细数 |
| 逾期明细数 | effectiveDueAt 早于当前时间且状态非 APPROVED/CANCELLED 的明细数 |
| 已完成视频数 | APPROVED 明细 requestedVideoCount 求和，或按真实审核通过文件数统计；二选一待确认 |

验收标准：

1. 指标卡可下钻到构成该指标的明细列表。
2. 指标查询和列表查询在同一筛选条件下口径一致。
3. 管理端不再出现前端写死的 60、300 等常量。
4. 业务时区明确，默认建议 Asia/Shanghai。

### FR-P0-11 通知

【规划需求】

MVP 至少支持站内通知：

- 新任务分配。
- 任务被撤销或转派。
- 今日到期/即将逾期。
- 成片审核通过。
- 成片审核驳回。
- 审核任务到达。

【待确认】是否接入企业微信、飞书、钉钉或其他外部通知渠道。

### FR-P0-12 审计日志

【规划需求】

审计范围至少覆盖：

- 需求创建、更新、取消。
- 分配、整单分配、撤销、转派、重开。
- 素材上传、下载。
- 成片上传、提交。
- 审核通过、驳回。
- 权限和业务字典变更。

每条日志至少包含 actorId、action、objectType、objectId、before、after、reason、requestId、ip/deviceContext、createdAt。敏感信息和临时签名 URL 不写入日志。

### FR-P1-01 批量操作与容量辅助

- 批量分配、批量转派。
- 展示人员待办量、今日交付量、逾期量和估算工作量。
- 根据难易度、视频数、截止时间和人员容量给出分配建议。
- 建议只辅助决策，不在缺乏可解释规则时自动覆盖调度员选择。

### FR-P1-02 报表与导出

- 按时间、业务线、店铺、负责人统计需求量、准时率、返修率和审核周期。
- 导出遵循当前筛选和数据权限。
- 大数据量导出异步生成并通知下载。

### FR-P1-03 配置化工作流

- 不同业务线可配置是否需要审核、审核角色、默认 SLA、素材必填项和驳回原因。
- 状态机变化必须保留版本，避免历史数据被新配置重解释。

---

## 10. 关键业务规则

### BR-01 需求单与明细

- 一个需求单至少包含一条需求明细。
- 分配、交付和审核以明细为最小执行单元。
- 整单操作是对单内明细的批量事务操作，不是另一套状态。

### BR-02 数量口径

- “需求单数”“明细数”“视频数”必须分别命名和统计。
- requestedVideoCount 必须为正整数。
- 汇总值由明细计算，前端不得自行维护不可校验的累计字段。

### BR-03 截止时间

- 使用完整 DateTime 和明确时区，不使用只有 MM/DD 的字符串。
- 明细 dueAt 为空时继承需求单 dueAt。
- 修改截止时间必须记录修改人、修改前后值和原因。

### BR-04 负责人

- 人员关联使用稳定 userId，不使用花名或显示名作为主键。
- 一条明细只能有一个当前主负责人。
- 协作人员、审核人员和创建人使用独立关系。

### BR-05 返修次数

- revisionCount 表示审核驳回后进入返修的次数。
- 单纯重新上传但未提交审核不增加返修次数。
- 同一版本的审核备注修改不增加返修次数。

### BR-06 删除

- 正式业务对象默认逻辑取消/归档，不物理删除。
- 上传文件按保留策略处理，审计和已引用版本不可静默删除。

---

## 11. 交互与异常处理

### 11.1 操作反馈

- 成功提示应明确对象、结果和下一步。
- 失败提示应区分权限不足、状态冲突、网络失败、文件失败和系统错误。
- 写操作失败时不乐观地永久移除列表项；刷新后必须恢复真实状态。
- 长耗时操作显示进度，不使用只有 2.6 秒的瞬时提示承载关键结果。

### 11.2 并发冲突

当对象 version 与服务端不一致时：

1. 服务端返回 409 和最新摘要。
2. 前端保留用户当前筛选。
3. 提示“需求已被其他人处理”，提供刷新并定位该需求。
4. 不自动覆盖他人操作。

### 11.3 空状态

至少区分：

- 当前确实无需求。
- 筛选后无结果。
- 数据加载失败。
- 无数据权限。
- 上游同步异常。

### 11.4 文件异常

- 上传中断可重试。
- 文件校验失败展示具体原因。
- 文件处理未完成时不能提交审核。
- 素材或成片 URL 过期时重新获取，不把永久 URL 暴露在页面数据中。

---

## 12. 产品指标与埋点

### 12.1 核心业务指标

| 指标 | 公式 | 用途 |
| --- | --- | --- |
| 分配时长 | firstAssignedAt - demandCreatedAt | 判断需求进入制作前的等待 |
| P50/P90 分配时长 | 分配时长分位数 | 避免只看平均值 |
| 准时交付率 | 截止时间前首次有效提交的明细数 / 到期明细数 | 衡量执行及时性 |
| 审核通过率 | 首次提交即通过的明细数 / 已审核明细数 | 衡量首次交付质量 |
| 返修率 | revisionCount > 0 的已审核明细数 / 已审核明细数 | 识别返工 |
| 审核时长 | reviewedAt - submittedAt | 识别审核瓶颈 |
| 撤销/转派率 | 发生撤销或转派的明细数 / 已分配明细数 | 识别分配准确性 |
| 待分配积压 | 当前 UNASSIGNED 明细数及其账龄分布 | 识别调度压力 |
| 逾期积压 | 当前逾期且未完成明细数 | 识别交付风险 |

【待确认】准时交付按“首次提交”还是“审核通过”判断。建议同时保留两个指标，避免把审核耗时归因给制作人员。

### 12.2 目标制定方式

首期上线前不建议直接写死商业目标。建议：

1. 使用真实数据运行 2 至 4 周形成基线。
2. 按业务线、难易度和视频类型分层分析。
3. 由业务负责人确认目标周期和目标改善幅度。
4. 目标变更保留版本和生效时间。

### 12.3 建议事件

| 事件名 | 触发时机 | 关键属性 |
| --- | --- | --- |
| demand_pool_viewed | 打开待分配池 | userId、scope、filters |
| demand_order_expanded | 展开需求单 | orderId、itemCount、sourcePage |
| assignment_submitted | 提交分配 | orderId、itemIds、targetUserId、mode |
| assignment_succeeded | 分配成功 | requestId、itemCount、latency |
| assignment_failed | 分配失败 | requestId、errorCode、conflict |
| assignment_revoked | 撤销回池 | itemIds、reasonCode |
| assignment_transferred | 转派 | itemIds、fromUserId、toUserId、reasonCode |
| material_downloaded | 下载素材 | assetId、itemId、result |
| delivery_upload_completed | 文件上传完成 | itemId、fileCount、totalSize |
| delivery_submitted | 提交审核 | itemId、deliveryVersionId |
| review_completed | 审核完成 | itemId、deliveryVersionId、result、reasonCode |

埋点不得携带成片签名 URL、访问令牌、个人敏感信息或无脱敏的审核备注全文。

---

## 13. 非功能需求

### 13.1 安全

- 所有接口使用身份认证、服务端授权和传输加密。
- 文件下载使用短时签名 URL 或受控下载流。
- 对上传文件执行类型、扩展名、内容、病毒/恶意文件和大小校验。
- 操作日志与业务日志不得记录密钥、令牌和签名 URL。
- 防止水平越权、批量枚举、重复提交和跨站请求伪造。

### 13.2 一致性

- 分配、整单分配、撤销、转派和审核使用事务或可证明的一致性方案。
- 所有写接口支持幂等 requestId。
- 使用对象 version 或 ETag 进行乐观锁。
- 同一账号不同设备看到服务端同一事实源。

### 13.3 性能

【规划建议，需按真实量级校准】

- 常规列表首屏接口 P95 不高于 2 秒。
- 关键写操作在不含大文件传输时 P95 不高于 1 秒。
- 1000 条以上结果使用服务端分页或虚拟化，不一次性渲染全量。
- 文件上传与业务提交解耦，展示明确处理状态。

### 13.4 可用性

- 核心写操作失败不得造成静默数据丢失。
- 服务端发布应支持回滚和数据库迁移回滚/前滚策略。
- 上游同步失败进入可重试队列并告警。
- 关键任务具备日志、指标和链路追踪。

### 13.5 兼容与适配

- MVP 定位桌面 Web。
- 建议支持企业当前标准 Chrome/Edge 的最近两个受支持版本。
- 当前页面最小宽度为 1200px；生产版应在 1280px 常用办公屏下完成核心操作。

### 13.6 可访问性

- 主要动作支持键盘操作和可见焦点。
- 弹层支持 Escape 关闭并恢复焦点。
- 状态不能只依赖颜色表达。
- 动效遵循 prefers-reduced-motion。
- 文件上传、审核结果和错误提示需被辅助技术识别。

---

## 14. MVP 交付拆解

### 阶段 0：业务口径与数据合同冻结

产出：

- 角色与组织权限模型。
- 需求单、明细、分配、素材、交付、审核数据字典。
- 状态机。
- 指标口径。
- 上游接口和文件存储方案。
- 历史样例字段迁移/废弃映射。

退出条件：

- 第 18 章所有阻塞项有明确 owner 和结论。
- 前后端 API 不再复用 difficulty、target、maintain、shoot 等旧语义字段。

### 阶段 1：真实需求池与分配闭环

产出：

- 登录和权限。
- 需求接入、校验和异常队列。
- 待分配池。
- 整单/单条分配。
- 全局/个人进度列表。
- 撤销和转派。
- 审计日志。
- 真实指标。

退出条件：

- FR-P0-01 至 FR-P0-05、FR-P0-09、FR-P0-10、FR-P0-12 通过验收。
- 浏览器刷新、跨设备登录和并发分配均以服务端数据为准。

### 阶段 2：素材、交付与审核闭环

产出：

- 素材权限和下载。
- 成片上传。
- 提交审核。
- 通过、驳回和返修。
- 站内通知。

退出条件：

- 从新需求接入到审核通过可完整走通。
- 所有版本和审核记录可追溯。

### 阶段 3：运营增强

产出：

- 容量辅助、批量操作。
- 报表与导出。
- 外部通知。
- 可配置工作流。

---

## 15. UAT 验收场景

| 编号 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- |
| UAT-01 | 多明细需求接入 | 上游有一个含 8 条明细的新单 | 提交两次相同幂等请求 | 只生成 1 个需求单和 8 条明细 |
| UAT-02 | 整单分配 | 需求单 8 条明细均待分配 | 分配给制作人 A | 8 条全部成功，产生 8 条有效分配和一次批量审计 |
| UAT-03 | 单条分配 | 需求单仍有 5 条待分配 | 只分配其中 1 条 | 该条进入 A 的任务，其余 4 条仍在池中 |
| UAT-04 | 并发分配 | 调度员甲乙同时打开同一明细 | 同时分配给不同人 | 仅一笔成功，另一笔收到 409 且不会覆盖 |
| UAT-05 | 个人数据隔离 | A、B 均有任务 | A 登录并尝试访问 B 的任务接口 | 页面不展示，接口返回 403 |
| UAT-06 | 多组任务完整性 | A 属于多个授权制作组 | A 打开个人页 | 展示 A 的全部有效任务，不只第一组 |
| UAT-07 | 撤销回池 | 明细为 ASSIGNED 且未提交 | 调度员填写原因并撤销 | 状态回到 UNASSIGNED，历史负责人和原因保留 |
| UAT-08 | 非法撤销 | 明细为 PENDING_REVIEW | 直接撤销 | 系统阻止并提示先取消审核或使用特殊流程 |
| UAT-09 | 素材下载权限 | A 有任务，B 无权限 | 两人请求同一素材 | A 获得短时地址，B 返回 403 |
| UAT-10 | 成片提交 | A 已上传有效文件 | A 确认并提交审核 | 生成版本 1，状态进入 PENDING_REVIEW |
| UAT-11 | 审核驳回返修 | 版本 1 待审核 | 审核人驳回，A 重新提交 | revisionCount 为 1，生成版本 2，版本 1 保留 |
| UAT-12 | 审核通过 | 版本 2 待审核 | 审核人通过 | 明细为 APPROVED，个人制作中指标减少 |
| UAT-13 | 指标下钻 | 今日有若干未完成视频 | 点击今日需交付卡片 | 列表明细求和与卡片值一致 |
| UAT-14 | 同步一致性 | 管理端和个人端不同设备登录 | 管理端分配任务 | 个人端在约定时限内出现任务，刷新后仍存在 |
| UAT-15 | 上传失败恢复 | 大文件上传中断 | 恢复网络并重试 | 不产生残缺审核版本，可继续或重新上传 |
| UAT-16 | 审计追溯 | 需求经过分配、转派、提交、驳回、通过 | 查看操作历史 | 按时间完整展示操作者、动作、对象和原因 |

---

## 16. 技术与发布建议

本章是产品约束，不替代详细技术设计。

### 16.1 前端

- 将当前集中在单个 main.jsx 的业务逻辑拆分为路由、页面、组件、领域状态和 API 层。
- 使用正式路由区分管理端、个人端和审核端，不再依赖 HTML 文件和固定 URL 判断身份。
- 清理旧字段兼容逻辑，页面只消费冻结后的 DTO。
- 为表格引入服务端分页、筛选状态持久化和错误边界。
- 保留当前按需求单聚合、展开明细、整单/单条分配的交互资产。

### 16.2 后端

- 以服务端数据库为唯一事实源。
- 分配和审核接口提供事务、幂等和乐观锁。
- 对列表、指标和导出使用同一权限过滤器与同一状态口径。
- 文件对象与业务记录分离，使用对象存储和受控访问。
- 支持事件通知或 SSE/WebSocket；若用轮询，必须有版本游标和退避机制。

### 16.3 测试与发布门禁

至少建立：

- 状态机和指标算法单元测试。
- 需求接入幂等、分配事务、审核并发的集成测试。
- 管理端分配、个人提交、审核驳回/通过的端到端测试。
- 权限越权测试。
- 文件上传失败与恢复测试。
- 构建、lint、类型检查和测试全部通过后才允许发布。

---

## 17. 风险与应对

| 风险 | 影响 | 建议应对 |
| --- | --- | --- |
| 旧字段语义继续沿用 | 指标错误、数据迁移困难 | 阶段 0 冻结新模型，禁止前端兼容字段成为 API 合同 |
| 把 URL 页面区分当权限 | 严重越权风险 | 服务端鉴权，按 userId 和组织范围过滤 |
| 需求单数、明细数、视频数混用 | 看板失真、运营决策错误 | 三类指标分开命名和计算 |
| 整单分配缺少事务 | 同单部分成功、重复劳动 | 服务端事务与冲突检测 |
| 上传按钮即完成 | 无法证明交付和质量 | 文件版本、提交动作、审核结果分离 |
| localStorage 作为数据源 | 跨设备丢失、无法协作 | 后端数据库为唯一事实源 |
| 撤销不留原因 | 责任不清、历史断裂 | 强制原因、Assignment 历史和审计 |
| 文件 URL 永久公开 | 素材和成片泄露 | 私有存储、短时签名、权限校验 |
| 样例阈值被当业务规则 | 方案失真 | 样例仅用于演示，真实口径由业务确认 |
| 宽表持续扩列 | 可用性下降 | 列配置、固定关键列、详情抽屉和服务端筛选 |

---

## 18. 待确认问题

### 18.1 阻塞开发的问题

1. 正式需求来自哪个系统？接入方式是 API、消息、Excel 导入还是人工创建？
2. 来源系统中什么字段构成唯一幂等键？需求是否存在版本和撤回？
3. 一个需求单的多条明细是否允许分给不同负责人？
4. “名称”“负责人”“运营线”三者的正式关系是什么？
5. 负责人是个人、制作组，还是两者都需要？
6. 难易度的正式取值范围、定义和维护人是谁？
7. “需求视频总数”“视频数量”“今日需交付数”分别以单、明细还是视频为单位？
8. “最后交付日期”是截止时间、最近一次交付时间，还是最终完成时间？
9. “二次修改数量”的准确含义是什么？建议统一为返修次数。
10. 哪些状态允许撤销、转派和整单撤销？已上传或待审核时如何处理？
11. 谁负责审核？是否需要多级审核、抽检或无需审核的业务线？
12. 审核驳回原因字典和必填规则是什么？
13. 成片和素材支持哪些格式、大小、时长、分辨率？保留多久？
14. 使用哪个对象存储和媒体预览服务？
15. 使用哪个统一身份、组织和权限系统？

### 18.2 不阻塞模型但影响后续范围的问题

1. 是否需要企业微信、飞书或钉钉通知？
2. 是否需要自动派单和人员容量建议？
3. 是否需要需求导出、日报和管理报表？
5. 是否与“内容发布”“团队绩效”等现有模块打通？
6. 预计日新增需求单、明细、视频文件和并发用户量是多少？
7. 历史数据是否需要迁移，当前前端样例是否只是设计稿数据？

建议在评审会上为每个阻塞问题指定决策人、结论日期和产出物。

---

## 19. 评审建议

建议按以下顺序评审，避免先讨论页面细节：

1. 业务对象：需求单、明细、负责人、制作组、素材、交付版本、审核记录。
2. 数量和时间口径：单数、明细数、视频数、截止时间、完成时间。
3. 状态机：何时算分配、制作、提交、返修、完成、取消。
4. 权限：谁能看、谁能分、谁能撤、谁能交、谁能审。
5. 上游和文件系统依赖。
6. 页面与交互。
7. 指标、埋点和目标。

评审通过的最低产物应包括：冻结的数据字典、状态机、权限矩阵、接口 owner、文件规则和 UAT 用例。

---

## 20. 工作区证据索引

| 证据 | 文件位置 | 说明 |
| --- | --- | --- |
| 项目名、版本、依赖和脚本 | package.json | clip-dispatch-workbench 0.1.0；仅 dev/build/preview |
| 管理端入口 | index.html | 页面标题为“中台需求管理 · 需求进度” |
| 普通用户入口 | user.html | 页面标题为“中台需求管理 · 我的需求进度” |
| 多页面构建 | vite.config.js | admin 和 user 两个入口 |
| 导航、表头和样例指标 | src/main.jsx 第 7-31 行 | 现有页面信息架构 |
| 待分配样例数据 | src/main.jsx 第 34-69 行 | 12 个样例需求单、每单 5-15 条明细 |
| 已分配样例组 | src/main.jsx 第 234-260 行 | 原型展示组和旧语义数据 |
| 固定普通用户与本地存储 | src/main.jsx 第 262-344 行 | userShop、localStorage 数据读写 |
| 跨标签页同步 | src/main.jsx 第 357-438 行 | BroadcastChannel、storage、轮询 |
| 撤销、分配和整单分配 | src/main.jsx 第 456-536 行 | 当前交互闭环 |
| 上传和打开需求的占位行为 | src/main.jsx 第 537-551 行 | 当前仅显示 toast |
| 待分配池聚合与展开 | src/main.jsx 第 725-891 行 | 整单与明细交互 |
| 已分配需求聚合与主表 | src/main.jsx 第 929-1190 行 | 汇总、展开、交付和撤销按钮 |
| 桌面宽表和最小宽度 | src/styles.css 第 13-14、101-127 行 | 当前桌面布局约束 |
| 生产构建 | npm.cmd run build | 2026-08-27 本地执行成功 |

---

## 21. PRD 结论

当前原型最有价值的资产是“需求单聚合 + 明细展开 + 整单/单条分配 + 个人查看 + 撤销回池”的交互骨架。生产化的关键不是继续增加表格列，而是先建立可信的业务对象、状态机、权限、文件版本和审核闭环。

建议以本 PRD 的阶段 0 为下一步：先确认第 18 章阻塞问题并冻结数据合同，再进入后端和页面开发。否则继续在现有字段兼容层上叠加功能，会扩大数据口径和迁移风险。
